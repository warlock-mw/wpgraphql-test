import Typography from "typography"

const typography = new Typography({
  baseFontSize: "18px",
  baseLineHeight: 0.8,
  headerFontFamily: [
    "Raleway",
    "sans-serif",
  ],
  bodyFontFamily: ["Raleway", "Noto Sans JP", "sans-serif"],
  includeNormalize: true,
})
export default typography